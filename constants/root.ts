//константы, которые ссылаются на точки входа компонента
const ROOT_MEMES = document.getElementById('memes');
const ROOT_HEADHER = document.getElementById('header');
const ROOT_EDITOR = document.getElementById('editor');